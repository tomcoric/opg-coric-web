DirectoryIndex index.html

RewriteEngine On

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [END,NE,R=301]

# Force www
RewriteCond %{HTTP_HOST} ^kulin-coric\.hr$ [NC]
RewriteRule ^ https://www.kulin-coric.hr%{REQUEST_URI} [END,NE,R=301]
